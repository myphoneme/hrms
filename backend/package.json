{
  "name": "teamora-backend",
  "private": true,
  "description": "Teamora backend: NestJS services and shared TypeScript packages (npm workspaces).",
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "engines": {
    "node": ">=20.20.2 <21"
  },
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "start:auth": "npm run build -w @teamora/platform && npm run start:local -w @teamora/auth-service",
    "start:requisition": "npm run build -w @teamora/platform && npm run start:local -w @teamora/requisition-service"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.6.0"
  }
}
