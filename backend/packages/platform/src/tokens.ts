/** Injection token for the service's ServiceConfig. */
export const SERVICE_CONFIG = Symbol('SERVICE_CONFIG');

/** Injection token for the service's PostgreSQL connection pool (a `pg` Pool). */
export const DB_POOL = Symbol('DB_POOL');
